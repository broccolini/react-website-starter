import styled from 'styled-components'
import {
  space,
  width,
  maxWidth,
  flex,
  color
} from 'styled-system'

const Span = styled.span`
  ${space}
  ${width}
  ${maxWidth}
  ${flex}
  ${color}
`

export default Span
